<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">

    <item android:id="@+id/action_broadcast" android:title="@string/action_settings"
        android:orderInCategory="100" app:showAsAction="ifRoom"
        android:icon="@drawable/broadcasticon">

        <menu>
            <item android:id="@+id/broadcast_individual" android:title="Individual"
                android:orderInCategory="100" app:showAsAction="never"
                android:icon="@android:drawable/ic_menu_search"/>

            <item android:id="@+id/broadcast_group" android:title="Group"
                android:orderInCategory="100" app:showAsAction="never"
                android:icon="@android:drawable/ic_menu_search"/>
        </menu>
    </item>
</menu>
